spring:
  application:
    name: discovery-service
  cloud:
    config:
      uri: http://localhost:8088
 spring:
  profiles: prod
  cloud:
    config:
      uri: https://config-server-ms.herokuapp.com 
